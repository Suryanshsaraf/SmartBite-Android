<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android.label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartBite">

        <!-- Main Activity (Splash Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activities -->
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.login.SignupActivity" />

        <!-- Customer Activities -->
        <activity android:name=".ui.customer.CustomerDashboard" />
        <activity android:name=".ui.customer.MenuActivity" />
        <activity android:name=".ui.customer.CartActivity" />
        <activity android:name=".ui.customer.OrderHistoryActivity" />

        <!-- Delivery Partner Activities -->
        <activity android:name=".ui.delivery.DeliveryDashboard" />
        <activity android:name=".ui.delivery.MapTrackingActivity" />

        <!-- Profile Activities -->
        <activity android:name=".ui.profile.ProfileActivity" />

        <!-- Maps Activity -->
        <activity android:name=".MapsActivity" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_MAPS_API_KEY" />

    </application>
</manifest>